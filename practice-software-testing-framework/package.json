{
  "name": "welcome-to-docker",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-particles": "^2.9.3",
    "react-scripts": "5.0.1",
    "tsparticles": "^2.9.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "test:ui": "playwright test tests/ui --project=ui-prod",
    "test:ui:prod": "playwright test tests/ui --project=ui-prod",
    "test:ui:local": "playwright test tests/ui --project=ui-local",
    "test:ui:smoke": "playwright test tests/ui --grep @smoke",
    "test:ui:regression": "playwright test tests/ui --grep @regression",
    "test:api": "playwright test tests/api --project=api",
    "test:api:smoke": "playwright test tests/api --project=api --grep @smoke",
    "test:api:regression": "playwright test tests/api --project=api --grep @regression",
    "test:api:headed": "playwright test tests/api --project=api --headed",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.3.1",
    "typescript": "^4.9.5"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
